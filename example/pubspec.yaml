name: example
description: Infinite ListView Example.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  infinite_listview_package:
    path: ../

flutter:
  uses-material-design: true